# 项目交接稿

更新时间：2026-08-17
项目目录：`E:\desktop\strj - 副本`

## 当前目标

继续排查并修复项目中的功能性 Bug。用户重点反馈过：

- 跨页切片只有一半或内容被截断；
- AI 解析能力弱、部分图片没有解析；
- 软件操作不够方便、美观；
- 需要快速审查大量题图，并为没有解析的题目补写解析。

## 已完成并提交

当前 Git 已建立，最近提交如下：

- `7403c36 chore: establish project baseline`
- `c2e53fa fix: repair study workflows and rebuild question bank`
- `1effafa fix: stabilize AI tutoring and practice state`
- `5c29451 fix: prevent cloud sync data loss and hide credentials`
- `e734b07 fix: repair crop audit and study workflows`
- `d07033a fix: harden exam planning and admin sync`
- `3b04712 fix: preserve study history and share team state`
- `1663b41 fix: purge deleted member data across devices`
- `bd4f82a fix: correct real-world answer grading`
- `b5a72e7 fix: prevent stale UI updates and paginate exports`
- `6711261 fix: merge concurrent team sync changes`
- `31600c8 fix: merge offline practice attempts`
- `3a65493 fix: sync account credentials safely`
- `ec41d2e fix: recover question bank loading failures`
- `9c043d2 fix: reset stale practice interactions`
- `8df4c49 fix: recover paper sharing failures`
- `1efa326 fix: prevent duplicate practice commits`
- `569ba4e fix: align module practice and streak stats`
- `365269f fix: preserve activity streak history`
- `7e3c0f6 fix: strengthen AI explanation quality`
- `4c54b79 fix: recover paper exports and bank loading`
- `71d3215 fix: recover local saves and cloud sync`
- `becad68 fix: recover drill navigation and image fallbacks`
- `988f3da fix: prevent duplicate bulk practice saves`
- `408497d fix: roll back partial account mutations`
- `f542a3c fix: recover corrupted local state`
- `d28a430 fix: repair cross-page answer crops`
- `18e4efd fix: isolate drill shortcuts from dialogs`
- `8c969c1 fix: recover stalled AI and offline notices`
- `2890d11 fix: isolate account authentication races`
- `b33558b fix: stabilize export modal lifecycles`
- `46415c8 fix: recover cloud sync and print exports`
- `e5cc9da fix: make Android builds installable`
- `4efb9b2 fix: harden exports practice and cloud recovery`
- `949d7e5 docs: record export and recovery hardening`
- `2c79da6 fix: improve mobile feedback exports and AI parsing`

主要完成内容：

1. 建立正式题库与题图审计流程：
   - `public/bank/questions.json`：479 道题；
   - `public/bank/chapters.json`：章节元数据；
   - `public/bank/crops/`：题目、答案切片；
   - `public/bank/crop_audit.html`：可视化切片审计报告；
   - `scripts/build_crop_audit_report.py`：生成审计报告；
   - `scripts/recover_known_answer_crops.py`：恢复已知错误切片。
   - `scripts/clean_question_text.py`：清除扫描页眉、页脚及串入文本；
   - `scripts/repair_placeholder_explanations.py`：为占位答案补写可直接使用的答案与解析。

2. 修复跨页/跨栏切片问题：
   - `六年级_ch14_超越篇_1_ans.png`：补全跨页答案；
   - `六年级_ch14_兴趣篇_5_ans.png`：补全跨页答案；
   - `六年级_ch17_拓展篇_2_q.png`：补全跨页题目；
   - `四年级_ch20_超越篇_6_q.png`：手工合成完整题图及图 20-32；
   - `四年级_ch20_超越篇_6_ans.png`、`_7_ans.png`：恢复官方答案图。

3. 为缺少官方解析的题目补写解析：
   - `四年级_2_兴趣篇_1` 已补充答案“左下4，右中3，右下6”；
   - 已补充 4 步具体说明；
   - 标记为 `needs_ai_explanation`，便于后续审查。

4. AI 辅导修复：
   - `src/services/aiTutor.ts` 对需要看图的题目自动选择视觉模型；
   - 关闭 DeepSeek/MiMo thinking，避免 900 token 全被 reasoning 消耗导致空响应；
   - 代理从 `.env.local` 读取密钥，密钥不再写入源码；
   - 视觉题识别条件扩展到“在图/图2”等描述；
   - 已在本地浏览器真实验证：视觉题能返回 4 步解析和最终答案。

5. 学习与云同步功能修复：
   - 修正用户记录、团队统计、艾宾浩斯阶段更新；
   - 修正登出后残留上一个账号数据的问题；
   - WebDAV 合并团队成员、公告和用户记录，避免整文件覆盖导致数据丢失；
   - 云端写入改为串行队列，避免慢请求覆盖新记录；
   - 开发环境使用 `/dav` 代理，避免浏览器直接暴露坚果云账号密码。

## 验证结果

最近一次验证结果（2026-08-17）：

- `npm test`：2 个测试文件、104 个测试全部通过；
- `npm run build`：构建成功；
- `npm run lint`：退出码 0，0 错误、0 警告；
- 本地浏览器回归：快速录入入口可达，题号按 1、2、3……正确显示；未登录保存会弹出登录框且不会误报成功；控制台无错误；
- Python 恢复/清洗/审查脚本均通过语法检查；
- 切片正式审计：479 道题，`image_issues=0`、`text_issues=0`、`missing_images=0`；
- 审计规则现在会分别检查缺图、异常宽高、题号与题库 ID 不一致、串入下一题、占位答案/解析、扫描页眉页脚等。
- Android：移动资源裁剪后约 102.5MB，最新调试 APK 113,219,924 字节，Gradle 构建成功，ADB 覆盖安装成功，应用冷启动成功，文件与系统分享插件已注册且未捕获崩溃日志。

## 最新一轮已完成

### 本次（`46415c8`）

1. 修复坚果云与 AI 接口在本地开发环境持续返回 HTTP 502：
   - 直接访问上游服务正常，故障定位为 Vite 内置代理在当前 Windows 环境转发失败；
   - 改为基于标准 `fetch` 的本地转发中间件，开发页面实测坚果云与 AI 接口均恢复 HTTP 200；
   - 坚果云账号、应用授权码和 AI Key 由被 Git 忽略的 `.env.local` 在构建时写入个人安装包，管理页默认可查看、可隐藏、可修改；
   - 正式安装包默认使用坚果云直连地址，旧浏览器保存的空配置会自动补回内置默认值。
2. 修复试卷/报告导出时 `html2canvas` 不支持 `oklch` 导致图片和 PDF 失败：导出克隆节点会先将相关颜色转换为兼容的 RGB/RGBA。
3. 打印样式排除评分框、导出框和图片预览框，系统打印预览不再把浮层盖在试卷上。
4. AI 双模型均失败时，优先显示题库中已核验的具体解析和答案，不再退化成泛泛的“审题、列式、验算”。
5. 补充移动端网页能力声明，消除 `apple-mobile-web-app-capable` 的弃用提示。

### Android 安装闭环（`e5cc9da`）

1. 找到旧 APK 膨胀到约 2.48GB 的根因：安装包误放在 `public` 后被再次嵌入新安装包。重复副本已移出静态目录，构建前检查会永久阻止 APK/AAB 再次进入 `public`。
2. 移动构建会自动去除原始扫描页、旧切片、审查页和调试素材，只保留运行所需的 479 道题、932 个题图引用；移动资源约 102.5MB。
3. 新 APK 为 104.82MB，包内嵌套安装包数量为 0；已通过 USB 覆盖安装到手机并冷启动成功。
4. `npm run cap:apk` 会自动选择 Android Studio 自带的 JDK 21，并兼容当前包含中文的 Windows 项目路径；`npm run cap:install` 可一键构建并安装到唯一已授权设备。
5. 正式 APK 启用 Capacitor 原生 HTTP，使坚果云 WebDAV 和 AI 直连绕过 WebView 跨域限制。
6. 根目录 `Q3考高斯刷题_v1.0.apk` 已替换为本次验证过的新包；旧重复包暂存在被 Git 忽略的 `tmp/quarantined-build-artifacts/`，不参与构建，可确认无须回退后手动清理。
7. 两个桌面入口已修复：Android Studio 脚本使用当前项目相对路径；本地网页脚本会等待服务真正就绪后再打开浏览器。

### 此前

1. 已完成一次只读 Bugbot 审查，并修复全部 3 个发现：
   - 拍照识别只有报告、没有保存入口；
   - 今日复习答对后题目移出队列，反馈随之消失；
   - 题库为空时快速录入页面会解引用空章节并崩溃。
2. `QuickEntryView` 已接入 `App.tsx` 与 `Navbar.tsx`，桌面和移动导航均可进入。
3. 批量录入现在只保存本次真正点过的题，不再把全章历史记录重复累计一次。
4. 快速录入、章节刷题统一使用 `calculatePracticeAttemptRecord`，不再重置错题阶段和错误次数。
5. 拍照识别增加“讲次、题目、正确/错误”的人工复核及保存同步闭环。
6. 修复照片读取异常处理和临时图片 URL 释放。
7. 修复题号显示误用 `section_num` 的问题；现在使用真实 `short_title`，并增加题库一致性测试。
8. 章节刷题深链接现在会等待异步题库加载完成，再打开指定讲次和题目；不会因首次数据为空而失效。
9. AI 解析按钮增加加载防重入、明确错误提示与重试入口。
10. 已从原书恢复 35 个已知错误/跨页答案切片和 12 个题目切片，包括：
    - 四年级第20讲多道跨栏、图形题与超越篇答案；
    - 四年级第2讲兴趣篇6等被图中数字误定位的答案；
    - 三年级第13讲超越1、第20讲超越4；
    - 五年级第15讲超越7；
    - 六年级第17讲超越4、第23讲概率题。
11. 清理了124道题、150个字段中的扫描页眉页脚/章节标题污染；后续重复运行清洗结果为0，脚本可幂等执行。
12. 为55道“见解析/见解答/详见原版名师精解”占位题补写了明确答案与步骤；当前题库不再存在占位答案或占位解析。
13. 批量审查不仅看图片尺寸，还检查题号与题库 ID，已因此发现并修复“四年级第20讲拓展13实际切成第9题”等隐藏错位。

## 上一轮已提交修复（`d07033a`）

1. 组卷随机排序由带偏差的随机 `sort` 改为 Fisher-Yates；不会修改原题库顺序，且测试可重复验证。
2. 组卷题数为0、总分小于题数时不再除零或产生负分；每题分值之和始终等于试卷总分。
3. 考试计划增加最后编辑时间，多设备同步时按最新编辑版本合并；旧版无时间戳数据仍可兼容。
4. 无论主动上传还是手动拉取，都不会再用旧考试计划无条件覆盖新计划；同步后界面会刷新为实际合并结果。
5. 管理员新增/删除成员、发布公告后会主动写入云端；管理员删除使用本机管理快照优先，不会被云端旧成员立即“复活”。
6. 首页、导航倒计时和导出的学情战报全部跟随用户设置的考试日期，不再写死为2026年9月18日。
7. 所有题目完成后，“剩余每日目标”会显示0题，而不是错误地继续显示1题。
8. 卡片刷题页的AI解析失败不再被静默吞掉；现在显示具体错误和重试入口，自动解析失败也不会产生未处理异常。
9. 新增5项回归测试；当前共21项测试全部通过，生产构建成功。

## 上一轮已提交修复（`3b04712`）

1. 修复错题本“标记已掌握”重建记录的问题；现在会保留错误次数、尝试次数、复习历史和个人笔记。
2. 统一兼容 `user_notes` 与旧版 `notes` 字段，章节刷题中编辑笔记不会造成旧笔记不可见。
3. 纸卷录分由逐题保存/逐题云同步改为整卷批量保存，只触发一次云同步；重复点击保存不会重复累计尝试次数。
4. 卡片刷题答对后的延时跳转会在手动翻页或切换筛选时取消，不会与“下一题”叠加导致跳过一道题；500毫秒内的重复判分也会忽略。
5. 键盘快捷键始终使用最新题目和输入状态，不再依赖缺失的 React Hook 依赖。
6. 团队成员和公告改为独立的 `shared_team.json` 权威云端快照，所有账号读取同一份共享数据；个人答题记录仍按账号隔离。
7. 新共享文件兼容旧版每用户文件；管理员删除会精确下发，普通成员本机残留的旧账号不会再次复活。
8. 团队错题榜区分“错误次数”和“出错人数”，不再把累计错误次数错误标为人数。
9. 组卷页补回已经实现但此前没有界面入口的“兴趣篇/拓展篇/超越篇”筛选；图片/PDF导出期间禁止重复点击。
10. 清理全部既存代码检查警告，包括失效导入、无效处理函数和键盘 Hook 依赖；当前代码检查为0警告。
11. 当前共23项测试全部通过，生产构建成功。

## 上一轮已提交修复（`1663b41`）

1. 注销成员会同时清理本机密码、练习记录、考试计划和该账号的登录会话，重新使用相同账号不会看到上一位成员的数据。
2. 被删除的默认示例账号不会被初始化逻辑重新生成练习记录。
3. 待删除的云端个人文件会记录到本机待办队列；管理员同步时调用 WebDAV `DELETE`，成功后才清除待办，临时网络失败后可以继续重试。
4. 已注销成员的旧设备在读取团队权威快照后会清理本机个人数据、退出登录，并停止重新上传已经删除的云端文件。
5. `App.tsx` 统一通过同步后状态刷新入口检查账号是否仍有效，避免页面继续显示已经注销的会话。
6. 新增2项账号生命周期测试并扩展云端删除测试；当前共25项测试全部通过。

## 上一轮已提交修复（`bd4f82a`）

1. 自动判分现在按小问顺序比较多个答案；`38,7` 不再被错误接受为“7人，38个”。
2. 兼容中英文逗号、分号、“(1)(2)”小问标记，包括题库中缺少分隔符的“(1)12种(2)21种”。
3. 增加带分数、中文分数、百分数和数值序列/比值的等价判定。
4. 补齐“平方千米、公顷、颗、枚、盆、根、级、页”等真实题库单位，避免正确数值因单位遗留而被判错。
5. 证明/构造/分类讨论、复合币值、OCR 断裂数字、截断答案和日期时间等不安全格式改为人工确认，不再猜测判分。
6. 已对 479 道真实题库答案做分类扫描：374 道可安全自动判分，105 道转人工确认；当前共28项测试全部通过。

## 上一轮已提交修复（`b5a72e7`）

1. 云同步回调增加当前账号校验；A 账号的慢请求在切换到 B 后返回，不再把 B 误退出。
2. 卡片刷题和错题复习只有在记录真正保存后才更新界面、庆祝或跳题；游客不再出现“看似成功、实际未保存”。
3. AI 题目解析和拍照识别增加请求代次校验；快速切题或连续选图时，较慢的旧结果不再覆盖当前内容。
4. 顶部考试日期改为跟随用户计划，不再写死为 2026 年 9 月 18 日；云端更新计划后，未打开的设置草稿也会同步，避免旧计划回写。
5. 学情战报总题数使用真实题库的 479 题，不再显示旧值 362。
6. 试卷和学情战报的长内容 PDF 改为按 A4 页高连续分页，不再裁掉第一页以后的内容。
7. 系统分享现在会等到图片生成和分享完成再解除按钮锁定；取消与失败提示不再误报为已下载。
8. 新增账号切换与 PDF 分页回归测试；当前共30项测试全部通过，构建和代码检查通过。

## 上一轮已提交修复（`6711261`）

1. 成员新增/删除改为按账号保存带时间的变更记录，不再依赖整份共享快照的“最后写入者获胜”。
2. 两个管理员离线各自新增成员后会按用户名合并，两边的新成员都保留；并发发布的公告也按唯一 ID 合并。
3. 删除成员会同步持久化的删除记录；旧设备上的过期成员快照不再能把该账号重新“复活”。
4. 同名账号在云端旧个人文件尚未删除时禁止重建；完成一次同步清理后才能重新添加，防止新账号继承上一位成员的云端练习数据。
5. 被其他管理员删除的管理账号也会在上传前被检测并退出，不能依靠旧的本机管理员权限继续覆盖共享数据。
6. 手动下载与后台上传纳入同一串行队列；刚新增成员/公告时同时点“立即同步”，不再发生下载覆盖未上传修改。
7. 云端首次无个人文件时在当前同步任务内直接初始化，避免嵌套队列造成永久等待。
8. 保持对旧版 `shared_team.json` 的兼容；新增4项并发、恢复和首次同步回归测试，当前共34项测试全部通过。

## 上一轮已提交修复（`31600c8`）

1. 用户练习记录增加按设备独立的尝试次数与错误次数计数器；同一账号在两台设备离线作答后，不再因整条记录择新覆盖而丢掉其中一次。
2. 云端合并按每台设备分别取最大计数，再汇总总次数；同一快照重复拉取或上传不会被重复累计。
3. 旧版只有 `attempt_count` / `wrong_count` 的记录会自动归入兼容基线，不要求手动迁移，也不会在第一次同步时翻倍。
4. 新增复习历史唯一标识；两台设备各自产生的复习记录会并集合并，重复同步仍只保留一份。
5. 增加离线分叉、重复同步、旧数据兼容及云端读写闭环3项回归测试；当前共37项测试全部通过。

## 上一轮已提交修复（`3a65493`）

1. 个人密码改为 PBKDF2-SHA256 单向凭据，并随团队账号变更安全同步；其他设备不再因缺少本机密码而错误接受默认密码。
2. 旧版明文密码在首次成功登录后自动迁移，迁移后删除本机明文；修改密码和管理员重置都会生成新的单向凭据并触发云端同步。
3. 未登录状态可独立拉取 `shared_team.json`；登录前优先刷新权威账号表，新设备上的新成员不再陷入“必须先登录才能同步账号”的死循环。
4. 应用远端成员删除时，会清理所有被删除账号的本机密码、练习记录和考试计划，不再只清理当前登录账号。
5. 新建账号增加不可复用的 `account_id`；离线设备即使错过删除过程，也能识别同名重建为新身份并清掉旧人的缓存数据。
6. 管理员账号重置使用其专用默认密码，普通成员仍使用 `123`；界面不会再提示一个随后必然失效的密码。
7. 新增跨设备密码、登录名单引导、远端删除、同名重建及凭据上传6项回归测试；当前共43项测试全部通过。

## 上一轮已提交修复（`ec41d2e`）

1. 题库加载统一检查 HTTP 状态、JSON 格式、非空数组、重复章节/题目 ID，以及题目引用的章节是否真实存在；发布缺文件或半更新时不再伪装成“0道题”。
2. 主界面新增明确的题库加载错误与“重新加载题库”按钮，不再只在开发者控制台静默报错。
3. 最近一次成功加载的约790KB题库会缓存到本机；断网或服务器临时异常时恢复有效副本，并在页面顶部明确提示当前使用缓存。
4. 管理后台把“导出 CSV”和“图片 / PDF / 分享”拆成两个入口；点击 CSV 不再同时弹出无关的图文报告窗口。
5. CSV 按标准转义逗号、引号和换行，并保护以 `= + - @` 开头的单元格，避免错列或被表格软件当作公式执行。
6. 新增题库网络加载、缓存恢复、HTTP失败、半部署校验及CSV转义5项回归测试；当前共48项测试全部通过。

## 上一轮已提交修复（`9c043d2`）

1. 错题复习页现在跟随实际题目 ID 重置输入、判分结果和答案展开状态；切换年级、列表或复习完成后，不会把上一题的答案状态带到下一题。
2. 拍照解析保存增加同步防重复锁；连续快速点击只会写入一次学习记录，不会重复累计尝试次数。
3. 只有真正保存成功后按钮才变为“已保存”；保存失败会解除锁定，用户仍可重试。
4. 选择一张新照片时会重置保存锁和成功状态，新的识别结果可以正常保存。
5. 当前共48项测试全部通过，生产构建成功，代码检查0错误、0警告。

## 上一轮已提交修复（`8df4c49`）

1. 试卷文字复制现在兼容剪贴板权限受限或现代 Clipboard API 不可用的环境，会自动尝试浏览器传统复制方式。
2. 复制失败不再产生未处理的异步异常；导出窗口会明确显示处理中、成功或失败原因。
3. 系统分享失败时自动降级为复制整卷文字与答案；用户主动取消分享会显示“已取消”，不会误报成功或失败。
4. 分享和复制期间统一锁定相关按钮，避免快速连点启动多个并发分享/复制任务。
5. 图片和 PDF 导出不再使用阻塞式弹窗提示结果，成功与失败均在导出窗口内持续显示。
6. 新增3项剪贴板主路径、权限失败降级和彻底失败回归测试；当前共51项测试全部通过。

## 上一轮已提交修复（`1efa326`）

1. 登录成功或关闭登录框时会清空密码；共享电脑上的下一位使用者不能直接沿用上一账号的完整凭据重新登录。
2. 登录过程增加请求代次取消；慢速账号同步期间点击关闭后，旧请求完成也不会在后台把用户重新登录。
3. 卡片刷题和艾宾浩斯复习改为“每道题每次进入只允许一次成功记账”；自动判分后再点手动结果不会重复增加尝试/错误次数或连续推进复习阶段。
4. 保存失败会释放单题锁，仍可重试；切换到另一题后正常开始新的记账周期。
5. 普通进入卡片刷题会清除旧的章节、题目或模块深链；退出及换账号也不会继承上一人的定位。
6. 矩阵录入与拍照录入使用独立成功状态，不再把另一入口的保存提示错误显示在当前页面。
7. 游客保存备考计划时会打开登录框并保留计划窗口，明确提示先登录，不再关闭窗口后静默丢弃修改。
8. 新增3项单题提交锁、失败解锁及刷题深链清理回归测试；当前共54项测试全部通过，生产构建和代码检查通过。

## 上一轮已提交修复（`569ba4e`）

1. 首页点击模块现在会进入该模块在所有年级、所有讲次中的完整题集，不再只取第一个匹配讲次；真实题库的“应用题”由错误的32题恢复为完整128题、覆盖4讲。
2. 模块专题模式在讲次选择框中明确显示“全部该模块讲次”和当前题数，也允许继续收窄到具体讲次。
3. 首页模块卡片改为根据当前题库动态生成，不再固定显示题库中实际为0题的模块；标题中的模块数量同步更新。
4. 连续打卡改为真正的“当前连续”：最新活动必须在今天或昨天；数月前的历史连续记录不再永久占据排行榜。
5. 未来时间的异常练习记录不会被计入当前连续天数，降低设备时钟错误造成的统计污染。
6. 新增模块全范围、可用模块、历史连续失效及真实128题/4讲范围3项回归测试；当前共57项测试全部通过。

## 上一轮已提交修复（`365269f`）

1. 每次普通正确作答也会追加带设备唯一 ID 的活动历史；同一道题连续多天练习时，较早日期不再被单个 `last_attempt_at` 覆盖。
2. 连续打卡同时读取每道题的最后作答时间与完整活动历史，并继续兼容旧版只有最后时间的记录。
3. 首页个人统计和团队排行榜改为传入当前题库的真实题目 ID；题库删除、改名或旧版本遗留的记录不再计入已刷题数、正确率、完成率和连续天数。
4. 完成率增加100%上限保护，即使兼容旧调用或异常数据也不会出现超过100%的进度条。
5. 新增同题跨三天连续练习与失效旧题过滤2项回归测试，并强化首次正确作答历史断言；当前共59项测试全部通过。

## 上一轮已提交修复（`7e3c0f6`）

1. AI 解析分流不再只依赖题干长度：数字谜、几何、明确标记为需 AI 解析的题，以及依赖插图的题目都会优先交给视觉模型，避免长题干把关键图形信息挡在模型之外。
2. 当题目同时有题图和官方答案/解析图时，两张图会带清晰标签一起发送给视觉模型；答案跨页或文字转写不完整时，模型仍能直接查看原图补全推理。
3. 对证明、分类讨论等不适合自动判分的复杂官方答案，不再完全隐藏；现在会作为“可能有 OCR 误差、必须独立验算”的参考材料提供，兼顾信息量和防止盲从。
4. 模型返回结果增加质量校验：步骤过少、拒答、空解析会自动换下一个模型；可自动核验的题目如果最终答案与官方答案冲突，也会拒绝该结果并继续重试，不再缓存明显错误解析。
5. AI 解析缓存升级到 v3，并把提示词版本、题图、答案图、模块和既有解析等纳入指纹；旧版低质量结果不会在修复后继续被长期复用，无效缓存也会自动剔除。
6. 缓存增加最近使用更新和内存上限，避免长时间使用后只限制写盘副本、内存仍不断增长。
7. 新增视觉分流、题图+答案图双图请求、复杂答案参考和错误答案模型回退等回归测试；当前共62项测试全部通过，生产构建和代码检查通过。

## 上一轮已提交修复（`4c54b79`）

1. 修复试卷图片/PDF导出按钮静默无反应：两个处理函数一直查找 `printable-paper`，但实际试卷节点没有该 ID；现在渲染节点与导出逻辑共用唯一常量，并在目标异常缺失时显示明确错误。
2. 用户自定义试卷标题会先清除 Windows/浏览器文件名中的非法字符，避免标题含冒号、斜杠、问号等符号时下载失败或文件名异常。
3. 年级、模块或篇章全部取消时不再错误地当作“全部选中”；现在会提示至少选择一项，且生成失败后会清掉旧试卷、旧录分和旧导出状态，避免把上一张试卷误当成新结果。
4. “仅从错题本抽题”在没有错题 ID 时会正确返回无匹配，不会退化为从整库抽题；错题数量只统计当前题库仍存在的题，旧版本残留记录不再污染显示。
5. 题库网络请求增加15秒超时；网络永久挂起时会自动恢复最近有效本机副本，没有副本时会结束加载状态并显示超时错误，不再无限转圈。
6. 题库校验增加页面渲染必需字段（年级、标题、模块、篇章、短标题、题干等），缺字段的半部署数据会在进入页面前被拦截；损坏的本机题库缓存会自动移除。
7. 学情战报的导出/分享失败现在使用红色错误提示和错误图标，不再用蓝色信息框加对勾伪装成普通提示；Safari等浏览器的分享取消也按通用 `AbortError` 正确识别。
8. 新增空筛选、空错题集、旧题过滤、导出目标、文件名清洗、缺字段题库和网络超时等7项回归测试；当前共69项测试全部通过，生产构建和代码检查通过。

## 上一轮已提交修复（`71d3215`）

1. 答题、批量录入和备考计划写入本机存储时增加统一故障捕获；存储满、无痕模式或浏览器禁止存储时不再抛出未处理异常，也不会误报成功或永久锁住提交按钮。
2. 页面顶部增加可关闭的持久化错误提示，明确区分“本机尚未保存”和“已保存在本机、但云同步失败”；用户可以根据提示释放空间、调整权限或手动重试同步。
3. 云同步结果不再被丢弃：后台上传返回失败或意外拒绝时会刷新红色同步状态并给出重试说明；后续同步成功会自动清除过期的云同步失败提示。
4. 所有坚果云请求增加15秒超时和中止信号；服务器永久不响应时会释放串行同步队列，后续新增答案仍能在网络恢复后继续同步，不再永远停留在“同步中”。
5. 同步状态写入改为尽力保存；即使本机恰好无法写入状态元数据，实际网络任务也会正常结束，不会因为保存一个“同步中/失败”标记而堵死整个队列。
6. 当前用户、练习记录、备考计划、成员、公告和同步配置的读取增加安全回退；浏览器完全禁止本机存储时应用仍能以只读/游客状态启动，而不是在首屏渲染前崩溃。
7. 登录、修改/重置密码、添加/删除成员、保存同步配置和发布公告统一识别存储空间与权限错误；管理员表单只有真正写入成功后才显示绿色成功状态。
8. 新增只读存储、配额耗尽和永久挂起 WebDAV 请求3项故障注入测试；当前共72项测试全部通过，生产构建和代码检查通过。

## 上一轮已提交修复（`becad68`）

1. 卡片刷题在筛选结果为空时会禁用底部“没做对/做对了”和前后翻题按钮，不再显示可点击但实际无反应的操作。
2. 当前题目改为使用经过边界约束的安全索引；题库热更新、筛选结果缩短或旧深链索引越界时，题号和实际题目会自动归位，不再出现“第6题/共2题”一类错误状态。
3. 答题输入、答案展开、AI结果及防重复提交锁改为按实际题目 ID 重置；同一位置替换成另一道题时不会残留上一题的输入或判分结果。
4. 没有原题切片时“原版切片”模式会禁用并自动显示文字题干；题图网络加载失败时也会退出空白图片模式并显示明确回退提示。
5. 官方答案切片加载失败时会自动触发 AI 文字解析，不再因为数据库中存在一个失效 URL 而误判为“已有解析”、留下空白答案区。
6. 艾宾浩斯复习保存后会自动选择队列中的下一道题；手动“掌握/强化”也会显示保存结果，只有一题时明确解释按钮被锁定是为了防止重复计数。
7. 新增索引边界和复习队列轮转2项回归测试；当前共74项测试全部通过，生产构建和代码检查通过。

## 上一轮已提交修复（`988f3da`）

1. 快速录入的矩阵保存增加同步提交锁；连续双击“一键保存”只会写入一次，不再把同一批对错结果重复累计到作答次数和复习历史。
2. 矩阵没有任何本轮改动时保存按钮会禁用；保存失败会释放锁供用户重试，保存成功后只有重新修改题目状态才会开启下一次提交。
3. 同一张纸卷成绩保存后，关闭并重新打开录分弹窗不再解除保存锁；原来这一操作会把整张试卷的所有题重复记账。
4. 已保存的纸卷分数会以只读状态重新展示；必须生成新试卷后才能开始新的整卷录分，避免误触修改一题却把整卷再次写入。
5. OCR 识别结果保存后会锁定讲次、题号和人工判分；界面不再允许切到另一题却继续显示“已保存”，录入下一题需明确上传新照片。
6. 新增可失败恢复的通用提交锁回归测试；当前共75项测试全部通过，生产构建和代码检查通过。

## 上一轮已提交修复（`408497d`）

1. 成员新增、密码修改和成员删除改为多键本机事务；成员表、密码、云端变更日志、待删除队列或学习数据中任一步写入失败时，会尽力恢复操作前的完整快照，不再出现界面报失败但账号已经被加上/删掉一半的状态。
2. 成员删除中途失败时不会提前清除该成员的练习记录、备考计划和当前会话；故障解除后可安全重试。
3. 当前登录用户修改密码时，会在同一事务中更新本机会话里的单向凭据；刷新页面后不会继续携带旧成员快照。
4. 成员列表读取增加结构校验并始终返回独立副本；存储被禁止或数据损坏时，调用方修改回退列表不会污染全局默认成员。
5. 管理后台禁止注销当前正在使用的管理员账号，避免管理员把自己的会话与云端账号置于竞态删除状态。
6. 新增成员新增/删除中途配额耗尽2项故障注入测试，并强化当前会话改密和只读回退断言；当前共77项测试全部通过，生产构建和代码检查通过。

## 上一轮已提交修复（`f542a3c`）

1. 手动坚果云同步增加独立提交锁和即时“同步中”状态；快速连点只会运行一个任务，异常抛出也会释放按钮并显示可重试错误，不再积压多轮重复拉取/回传。
2. 管理员公告发布增加同步提交锁；双击提交不会生成两条相同公告，失败会解锁重试，成功后必须开始新草稿才能再次发布。
3. 云同步配置表单会在用户未编辑时跟随最新配置更新；保存时只覆盖地址、账号、授权码、API Key 和自动同步开关，不会把刚完成的同步状态、时间或错误信息覆盖成旧值。
4. 当前用户、练习记录、备考计划、公告、待删除账号和同步配置读取增加运行时结构校验；能解析但类型损坏的 JSON 不再直接进入统计、复习或同步逻辑。
5. 损坏练习记录会逐题保留仍有效的题号、状态和计数，同时移除非法复习阶段、历史和设备计数；单条坏数据不会再拖垮整位用户的记录。
6. 损坏备考计划会逐字段回退默认值，非法日期不再造成倒计时空白；损坏同步配置不会让 URL 等字段以数字/对象进入网络函数。
7. 新增可解析但结构损坏的全套浏览器缓存回归测试；当前共78项测试全部通过，生产构建和代码检查通过。

## 最新一轮已提交（`d28a430`）：题库跨页与串题切片修复

1. 从原书 PDF 重新生成整套题库切片，再应用已确认的跨页坐标，避免在已经截坏的图片上继续裁剪；当前题库仍为479题。
2. 新增基于答案页黑底题号方块的边界修复脚本，可自动识别一个答案图中混入多个题号的情况，并将当前题答案裁到下一题之前。
3. 修复已知跨页和错位图片，包括六年级第14讲兴趣篇第5题、超越篇第1题，六年级第17讲拓展篇第2题，以及四年级第2讲兴趣篇第4、5题。
4. 25个无法可靠恢复的整行、跨栏或仅剩续页的错误答案图不再展示；题库改用已人工撰写的完整答案与解析，避免把邻题内容当作本题答案。
5. 补全六年级第14讲、第17讲、第23讲等重点题目的题干、答案和分步解析；当前共有26题使用人工撰写解析兜底。
6. 审计报告增加答案图异常宽、多个题号、题号出现过晚和题干以冒号结尾等规则；最新结果为479题、0图片异常、0文本异常、0缺图、0跨题答案图。
7. 题库测试增加PNG宽度校验，防止未来再次把双栏整行截图作为单题答案发布；当前共79项测试全部通过，生产构建和代码检查通过。

## 最新一轮已提交（`18e4efd`）：快捷键、弹窗与日期边界修复

1. 卡片刷题的全局快捷键不再拦截登录框、修改密码框等弹窗中的回车和数字键，避免无法提交表单或在弹窗后方误判题目。
2. 输入框、下拉框、按钮、链接和可编辑区域中的按键不再触发翻题、判对/判错或展开答案；只有刷题答案输入框中的回车会提交答案。
3. 登录、修改密码和图片查看器增加明确的模态窗口标记，刷题快捷键可可靠识别并暂停。
4. 图片查看器切换图片或重新打开时会恢复100%缩放和0度旋转，并支持Esc关闭，不再继承上一张图片的显示状态。
5. 备考日期现在严格校验真实日历，`2月31日`、非闰年的`2月29日`等日期不会被JavaScript静默滚到3月；倒计时按本地日历日期计算，避免夏令时导致多算一天。
6. 新增3项快捷键和日期边界回归测试；当前共82项测试全部通过，生产构建和代码检查通过。

## 最新一轮已提交（`8c969c1`）：离线公告与AI卡死恢复

1. 拉取团队共享数据时不再直接用云端公告列表覆盖本机列表；离线发布但尚未上传的通知会与云端公告按ID合并，网络恢复后仍可继续进入上传队列。
2. AI文字解析、视觉解析、题目/答案图片读取和拍照判卷统一增加30秒请求超时；模型或网络永久无响应时会结束等待、显示回退结果并允许再次尝试。
3. 超时会主动中止仍在进行的网络请求，避免用户多次重试后积压大量失效连接。
4. 新增离线公告拉取与AI永久挂起2项回归测试；当前共84项测试全部通过，生产构建和代码检查通过。

## 最新一轮已提交（`2890d11`）：登录、改密与账号复用竞态修复

1. 成功登录的最后登录时间现在与账号凭据一起写入团队成员变更日志，下一次云同步会把新时间传播到其他设备；管理端不再长期显示旧登录时间。
2. 登录校验在耗时的密码验证前后都会核对账号身份和凭据快照；同名账号在验证期间被删除并重新创建时，旧登录请求会安全失败，不能误进入新账号。
3. 没有单向凭据的旧账号会在成功登录时一次性完成账号 ID、密码哈希、登录时间、成员列表、变更日志和当前会话更新；其中任一步本机写入失败都会整体回滚。
4. 当前用户改密改为带会话身份保护的单一服务操作；验证或生成密码哈希期间如果退出、切换账号或云端密码已变化，本次修改不会串写到旧账号或新会话。
5. 管理员重置密码也会绑定操作开始时的账号身份和凭据；同名账号在计算密码哈希期间被复用时，不会把新用户的密码意外重置。
6. 修改密码窗口在保存期间禁用关闭、取消和输入，账号切换时立即清空；成功后的延迟关闭计时器会在手动关闭、重新打开或组件卸载时清除，不再误关后来打开的新窗口。
7. 新增登录时间共享、同名账号复用、跨会话改密、正常改密和管理员重置竞态5项回归测试；当前共89项测试全部通过，生产构建和代码检查通过。

## 最新一轮已提交（`b33558b`）：导出弹窗与异步提示生命周期修复

1. 管理端学情战报关闭后会清除上次导出、分享或失败提示；重新打开不再展示已经过期的成功/错误状态。
2. 学情战报和试卷的图片、PDF、复制及系统分享统一增加同步操作锁；在界面来得及变成“处理中”之前快速连点，也只会启动一个任务，不会重复下载或调起多个分享窗口。
3. 导出进行期间会锁定关闭、取消和打印按钮，避免渲染节点在截图/PDF生成中途消失，或系统打印与画布捕获同时运行导致空白、失败及状态串到下一次打开。
4. 试卷导出弹窗增加明确的模态窗口语义，与刷题快捷键隔离规则保持一致。
5. 同步配置保存和公告发布的短暂成功提示改为“只保留最后一个计时器”；连续保存或发布时，上一轮计时器不会提前清掉新提示，离开管理页时也不会留下卸载后的回调。
6. 新增可替换、可清理临时提示计时器的回归测试；当前共90项测试全部通过，生产构建和代码检查通过。

## 最新一轮已提交（`4efb9b2`）：导出、随机刷题、数据恢复与多管理员并发修复

1. 试卷“高清长图”现在把全部 A4 渲染页纵向拼接为一张 JPEG，不再下载多张图片；同时加入最大像素与最大高度约束，避免超长试卷撑爆浏览器画布。
2. 试卷 PDF、长图和学情海报统一使用支持现代颜色的渲染器，彻底移除 `oklch` 兼容补丁；网页端和 Android 端共用文件交付服务，手机端通过原生文件与分享插件交付。
3. 电脑端微信分享改为把 PNG 海报直接复制到剪贴板，并提示在微信电脑版聊天框按 `Ctrl+V`；不支持剪贴板时自动降级下载。
4. 学情战报标题固定分为“考试日期”和“全员学情战报”两行，避免日期、间隔点与标题挤在同一行造成错位。
5. 卡片刷题新增随机模式，可选“完全随机、未做优先、错题优先”，范围可选“当前筛选、当前讲次、当前年级、完整题库”；随机历史支持返回上一题，且不会立即重复当前题。
6. 内置 220 条数学主题名言、148 位不同作者，首页每 15 秒随机轮播并可手动换一条；数据来自 Project Gutenberg 公版数学名言集，生成脚本支持断点续跑与去重。
7. 管理员删除成员改为云端软删除：个人记录保留并写入 `deletion.isDeleted`、删除时间、操作者和可恢复标记，不再发送 WebDAV `DELETE`；管理页增加“账号回收站”，可恢复原账号身份、密码凭据和数据。
8. 同名账号复用会核对 `account_id`，新账号不能读到旧账号的软删除数据；恢复原账号后可继续合并原记录，下一次正常上传会清除删除标记。
9. 多管理员共享存储增加版本冲突控制：读取 `shared_team.json` 时保存 ETag，写入使用 `If-Match`；遇到 409/412 会重新拉取、合并成员变更、公告和账号操作后自动重试，避免最后写入者覆盖另一位管理员。
10. 公告 ID 改为 UUID；同一账号、相同时间戳的并发操作增加确定性胜负规则，所有设备最终会收敛到同一结果，删除与新增同刻冲突时删除优先。
11. 已通过真实浏览器回归：长图仅生成 1 张、随机策略与跨范围切题正常、电脑端微信复制成功、标题排版正常、首页显示 220 条名言库。
12. 已模拟两个管理员同时写入：第一次上传返回 412，客户端重新拉取另一管理员的新成员并合并重试，最终双方新增内容都保留；软删除测试确认全过程没有物理 DELETE。
13. 最新验证为 2 个测试文件、100 项测试全部通过，代码检查 0 错误，生产构建成功，479 道题与 932 个题图引用检查通过。
14. 该轮 APK：`Q3考高斯刷题_v1.0.apk`，大小 113,219,924 字节，SHA-256 为 `15FF619CFD69A87DBF6E8D602BFACF8A93F4453ADDBE65CA2F828FA6B1A45D05`；已通过 ADB 安装并冷启动，Android 进程正常。

## 最新一轮已提交（`2c79da6`）：手机交互、AI 结构清洗与导出体验修复

1. 手机端智能精讲按钮改为固定方形图标，不再占用底部“做错/做对/下一题”的横向空间；解析中或失败时在底部操作栏上方显示独立浮层，状态不再因为正文滚出屏幕而不可见。
2. 点击已有解析会直接滚动到精讲卡片；新请求成功后也会定位到结果。无答案图时的自动解析增加运行中/失败保护，避免按钮请求与自动请求重复并发。
3. 章节刷题补齐加载提示、请求过期隔离和完整结构化精讲，现会展示核心思路、3—6 步推导、最终答案、教学避坑与回退警告，不再只显示一段笼统心得。
4. AI 返回被截断、重复或缺少结束括号时，会从不完整 JSON 中逐字段恢复可用内容；所有可见字段都会过滤 `analysis`、`stepByStepSolution`、`finalAnswer` 等提示词骨架，旧缓存若仍含外溢字段会自动失效。
5. AI 质量门槛提高为至少 3 个有效步骤；文字模型均返回 502 时，会使用原题与官方答案切片尝试视觉模型，仍失败才回到题库中已核验或人工撰写的解析。
6. 手机端试卷直接分享改为一次连续画布渲染和受控 JPEG 编码，避免逐块截图再拼接造成长时间等待；高清长图仍保持单个文件输出。
7. 管理端 CSV 在 Android 上改用原生文件与系统分享交付，并显示“准备/导出/取消/失败/完成”状态；桌面浏览器继续直接下载。
8. 所有头像统一只显示姓名中的第一个汉字，英文姓名才回退到账户首字母，避免长姓名、表情或多字符撑坏头像布局。
9. 已生成两份中文 PPT：`output/pptx/项目功能介绍.pptx`、`output/pptx/项目迭代过程介绍.pptx`；后续已在下一轮重构为 21 页截图丰富版，详见下节。
10. 最新验证为 2 个测试文件、104 项测试全部通过；代码检查 0 错误，生产构建成功，479 道题与 932 个题图引用检查通过。
11. 最新 APK：`Q3考高斯刷题_v1.0.apk`，大小 113,219,924 字节，SHA-256 为 `5C455EA4F01684B1188C9845FB095A8CB5AAA8FEE40705EE2A33E2B0A536E803`；已覆盖安装、冷启动并确认进程、应用标题、卡片刷题入口正常。
12. Android 真机进一步确认：AI 图标按钮尺寸为 159×160 像素；点击后独立加载浮层可见，按钮同步进入旋转等待状态，未挤压两侧主操作按钮。

## 最新文档交付：截图丰富版项目介绍 PPT

1. `output/pptx/项目功能介绍.pptx` 已扩充为 12 页，使用本地真实页面截图完整介绍首页、题库与原书切片、随机刷题、卡片判分、AI 结构化精讲、艾宾浩斯错题、矩阵录分、拍照判卷、智能组卷、PDF/单张长图/系统打印/跨端分享、团队学情、成员管理和手机端使用链路。
2. `output/pptx/项目迭代过程介绍.pptx` 已扩充为 9 页，使用用户提供的跨页切片、半张答案、海报排版和 `oklch` 导出报错截图，与修复后的题库、AI、打印、同步和移动端页面做前后对照。
3. 两份 PPT 共 21 页，全部为中文；每页均含 `[Sources]` 演讲者备注，逐页 PNG 预览已人工检查，并通过 `slides_test.py` 越界检测。
4. 管理配置页会显示坚果云授权码与模型 API Key，因此没有放入 PPT；采集时生成的含密钥临时截图已立即删除。演示稿只使用成员管理、同步状态和数据恢复相关的安全页面。
5. 两份 PPT 由 `tmp/presentation-build/build_decks.mjs` 使用 `@oai/artifact-tool` 生成；真实截图素材位于被忽略的 `tmp/ppt-assets/`，最终交付只提交 PPTX。

## 最新一轮：跨设备记录、自动 AI 与浏览器分享修复

1. 个人学习记录的 WebDAV 上传现在会使用 GET 返回的 ETag 发送 `If-Match`；云端文件不存在时使用 `If-None-Match: *`，409/412 冲突会重新拉取另一台设备的新记录、合并并最多重试 3 次，不再只靠单个浏览器进程内队列防覆盖。
2. 云端账号软删除也采用相同的条件写入与冲突重读逻辑；如果删除标记写入期间另一台设备刚上传新题目，重试会保留最新记录再加删除标记，仍不执行物理 DELETE。
3. 无答案切片题的自动 AI effect 不再依赖并清理自身刚设置的 `isAiLoading`；改用“题号 + 请求编号”隔离重复请求和过期结果，切题、卸载或手动请求时旧结果不会串到新题。
4. 真实浏览器已验证三年级第13讲兴趣篇第1题：展开答案后自动生成 qwen3.7-plus 的核心思路、5步推理、最终答案和教学避坑，约1.5秒结束加载并恢复“查看精讲”，未再永久卡住。
5. 移动浏览器只有在 `navigator.canShare({ files })` 明确支持文件时才调用文件分享；缺少能力、文件类型不支持、用户激活失效或分享窗口抛错时，都会回退下载，不再误报“已分享”却拿不到文件。
6. 新增个人记录 ETag 冲突合并、缺少 `canShare` 和分享失败回退 3 项测试；当前 2 个测试文件、107 项测试全部通过，代码检查 0 错误，生产构建成功，479 道题与 932 个题图引用检查通过。
7. 最新 APK 已覆盖安装并启动，进程号验证成功；`Q3考高斯刷题_v1.0.apk` 大小 113,219,924 字节，SHA-256 为 `3983B938131DD0627574D0968B52C4320317042BBF9C65CF2E89A2BB67B61745`。

## 最新发布交付：v1.0.0 内部全平台材料

1. 最新完整交付目录为 `output/release/Q3考高斯刷题_v1.0.0_内部发布材料_最终版/`，共 33 个文件、32 条 SHA-256、884,001,687 字节；包含 Windows 安装/免安装、macOS Apple 芯片/Intel、Android、网页包、两份中文 PPT、14 张真实功能截图，以及内容一致的一页 A4 使用指南 PDF 与高清 PNG。旧目录曾被 Windows 占用，因此最终版使用独立目录，避免覆盖失败。
2. Windows 安装版为 188,126,785 字节，SHA-256 `348C0F3C2202F405156703C597D3F7394B8F597FEA9FBCA8A2DEEFCCE96A611F`；免安装版为 187,884,813 字节，SHA-256 `6E14DE14BFF9873428D876E84907FAF8B2F621CFC78D7B6E6F51763BA201E384`。二者均为 Electron 独立应用，无需 Node.js。
3. macOS Apple 芯片 `arm64` 包为 143,074,423 字节，SHA-256 `81F92450919F771FDE9D3489EBA2B5F96621583848ED09274D07A2EE5672B517`；Intel `x64` 包为 144,065,843 字节，SHA-256 `891D940EE4B74618DF297A1C5B7A29A476FCF3516A37CBF5653D6C0F66729283`。两包内置经官方 SHA-256 校验的 Node.js v24.19.0 运行时，无需 Homebrew、Node.js 或开发工具；审计核对了 Mach-O 架构和 479 道题。
4. Android 内部版为 113,219,924 字节，SHA-256 `954B2CFAEF6BE13BB8856719F312B6B2FDC179EC39EF565ED3187894FF17B1FB`；用于内部侧载，不是应用商店正式签名包。
5. 网页部署包为 102,956,365 字节，SHA-256 `3F1BE500DC82C92DA660F48AB936E43F724142CE9EED7E85AFB5B1D7D3D69FB2`。Windows 用户双击启动脚本即可：自动检测 Node.js，缺少时下载官方免安装运行时并校验 SHA-256，启动浏览器并创建桌面快捷方式；另含停止脚本。底层 PowerShell 脚本使用纯 ASCII，已通过 Windows PowerShell 兼容解析。发布审计真实解压并启动服务，首页和题库均返回 200，题库为 479 道。
6. Windows、两种 macOS、Android 和网页资源均逐包解压确认内嵌 `NUTSTORE_USERNAME`、`NUTSTORE_PASSWORD`、`OPENCODE_API_KEY` 三项运行配置；审计只输出配置名称，不输出实际值。`.env.local` 继续忽略，Git 跟踪文件逐一确认不含真实值。
7. `npm run release:internal` 可重复生成全平台内部材料；`npm run release:audit` 会核对 33 个文件、32 条校验码、各平台架构、题库、配置写入和网页实际运行结果。
8. 这些包仅限受控内部设备，不能上传公开网盘、B站附件、应用商店或公共仓库。B站可使用 PPT 与安全截图做功能展示，但不得附内部包下载。
9. 微信小程序技术上可接入；快速体验可用企业主体小程序的 `web-view` + HTTPS 业务域名，正式版应增加后端中转坚果云和 AI 请求，不能把长期凭据内嵌到可下载的小程序包中。
10. 管理员名单现支持直接编辑既有成员姓名和角色；保存会写入带时间的 `upsert` 变更并同步到共享名单，内置 `admin` 不能降权，当前管理员界面也禁止误降自己的角色。新增回归后共 108 项测试通过。

## 当前未完成/下一步优先级

### 中优先级

- 继续抽样目视检查图片语义与题号对应关系，自动审计只能证明结构、尺寸和可识别文本未触发规则；
- 根据真实使用反馈拆分体积较大的 PDF/图片导出依赖。

### 后续抽查建议

- 当前自动审计为0异常，但“图片内容语义是否对应本题”不能只靠尺寸完全证明；后续新增题库时仍应打开 `crop_audit.html` 做抽样目视复核；
- 恢复脚本中的坐标基于当前这套原书 PDF，若更换扫描版本或 DPI，应重新目视校准。

## 重要文件

- `src/App.tsx`
- `src/components/Navbar.tsx`
- `src/views/QuickEntryView.tsx`
- `src/views/EbbinghausErrorView.tsx`
- `src/views/ChapterDrillView.tsx`
- `src/services/aiTutor.ts`
- `src/services/ebbinghaus.ts`
- `src/services/passwordCredential.ts`
- `src/services/questionBank.ts`
- `src/services/csvExport.ts`
- `src/services/practiceSession.ts`
- `src/services/fileDelivery.ts`
- `src/services/paperPageRenderer.ts`
- `src/services/mathQuoteRotation.ts`
- `src/services/storage.ts`
- `src/services/webdav.ts`
- `src/data/mathQuotes.ts`
- `tests/core-bugfixes.test.ts`
- `tests/question-bank.test.ts`
- `public/bank/crop_audit.html`
- `scripts/build_crop_audit_report.py`
- `scripts/recover_known_answer_crops.py`
- `scripts/repair_answer_crop_boundaries.py`
- `scripts/clean_question_text.py`
- `scripts/repair_placeholder_explanations.py`
- `scripts/generate-math-quotes.mjs`

## 运行与安全注意事项

- `.env.local` 已被 `.gitignore` 忽略，不能提交；
- 不要在终端输出或交接稿中写入 API key、坚果云用户名或密码；
- 用户曾粘贴过 API key；真实值只保存在被忽略的 `.env.local` 和生成的内部包中，不得写进源码、日志、截图、文档或 Git；
- 内部包按用户要求内嵌统一配置，因此拿到包的人理论上可提取凭据；只能在受控成员和设备间分发，B站只能发布演示素材，不能附内部包；
- 完成修改后再次运行测试、构建，并检查 `git status --short`，确认没有意外提交 `.env.local`。

## 推荐继续顺序

1. 读取本交接稿，检查 `git status --short`；
2. 如题库文件有变化，依次运行恢复、解析修补、清洗和审计脚本；
3. 打开 `public/bank/crop_audit.html`，使用“仅图片问题/仅文本问题”筛选，并抽查语义对应关系；
4. 继续审查导出、离线恢复和空数据状态等未覆盖的交互边界；
5. 运行 `npm test`、`npm run build`、`npm run lint`。
